<!DOCTYPE html>
<html>
    <%- include("head", { title: "DuckDuckGo docs | LetovoArchive", description: "DuckDuckGo documents on the Letovo Archive" }) %>
    <body>
        <%- include("header") %>
        <h1 class="title center">DuckDuckGo docs</h1>
        <h3 class="title center">a. k. a. letovo documents that may not be available from the website, but have been found on DuckDuckGo</h3>
        
        <h2 class="title center">Page <%= page %></h2>
        <div class="recent">
            <% ddgDocs.forEach(doc => { %>
                <div>
                    <a href="/ddg/<%= doc.id %>">
                        <%= (doc.name) %>
                    </a>
                </div>
            <% }); %>
        </div>
        <%- include("pagination", { page, lastPage }) %>

        <%- include("footer") %>
    </body>
</html>